/** Application version - single source of truth */
export const APP_VERSION = "v0.1.0";

/** Default page size for photo grid pagination */
export const PAGE_SIZE = 40;

/** Debounce delays (ms) */
export const DEBOUNCE_SEARCH_MS = 300;
export const DEBOUNCE_NOTES_MS = 800;

/** Polling intervals (ms) */
export const POLL_INDEXING_MS = 2000;
export const POLL_IDLE_MS = 15000;
export const POLL_SIDECAR_MS = 30000;

/** Batch sizes */
export const BATCH_SIZE_DEFAULT = 20;
